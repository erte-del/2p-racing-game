[plugin]

name="Track sources"
description="Ships each track's own text in an export, so a release computes the same track signatures as the editor."
author="erselinankul"
version="1.0"
script="plugin.gd"
